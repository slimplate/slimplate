{
  "name": "@slimplate/mono",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "admin": "lerna run start --scope @slimplate/admin-static",
    "build": "lerna run build"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1",
    "npm-run-all": "^4.1.5"
  }
}
